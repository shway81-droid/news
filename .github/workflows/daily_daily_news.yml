name: Daily Newspaper Bot

on:
  # 매일 오전 9시 (KST) = 0시 (UTC) 실행
  schedule:
    - cron: '0 0 * * *'

  # 수동 실행 가능
  workflow_dispatch:

permissions:
  contents: write  # 무안 매물 monitor 의 state.json 커밋용

jobs:
  send-daily-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run daily news bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python main_daily.py

      - name: Run mokpo illo monitor
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.MUAN_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.MUAN_CHAT_ID }}
        run: python monitor.py

      - name: Commit updated monitor state
        run: |
          if ! git diff --quiet state.json; then
            git config user.name  "monitor-bot"
            git config user.email "monitor-bot@users.noreply.github.com"
            git add state.json
            git commit -m "state: $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          else
            echo "state.json unchanged."
          fi
